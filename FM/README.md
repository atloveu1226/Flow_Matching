To be added...

Run the code by:
```bash
python train.py --config custom_configs/esd.py \
    --mode=train \
    --device=cpu \
    --num_steps=100
```

For images run:
```bash
python train_img.py --config custom_configs/image.py \
    --mode=train \
    --device=cuda
```
